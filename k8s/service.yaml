apiVersion: v1
kind: Service
metadata:
  name: polymarket-arb-api
  namespace: default
  labels:
    app: polymarket-arb-bot
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: polymarket-arb-bot
